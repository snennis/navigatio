name: frontend
description: "Navigatio - OpenStreetMap Karten App mit Hell/Dunkel-Modus und benutzerdefinierten Markern"
publish_to: 'none' # Private App - nicht auf pub.dev veröffentlichen

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # OpenStreetMap integration
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  
  # Location services
  geolocator: ^12.0.0
  
  # HTTP requests
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
